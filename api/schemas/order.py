"""Extended order schemas — kept for backward compat, primary schemas are in __init__.py."""

# This file is intentionally minimal.
# All order schemas are defined in schemas/__init__.py to avoid duplication.
# Import from there:
#   from schemas import OrderCreate, OrderResponse, OrderDetailResponse, OrderTrackingResponse
